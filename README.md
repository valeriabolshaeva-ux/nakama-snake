# 🐍 Snake Game

Классическая игра Змейка с современным дизайном и таблицей лидеров.

![Snake Game](https://img.shields.io/badge/Game-Snake-4ade80?style=for-the-badge)
![Python](https://img.shields.io/badge/Backend-Python-3776ab?style=for-the-badge)
![React](https://img.shields.io/badge/Frontend-React-61dafb?style=for-the-badge)

---

## 🎮 Особенности

- 🎨 **Красивый неоновый дизайн** с анимациями
- 🏆 **Таблица лидеров** — соревнуйся с другими игроками
- ⚡ **Увеличение скорости** по мере набора очков
- 📊 **Статистика** — отслеживай свой прогресс
- 💾 **Сохранение результатов** в базу данных

---

## 🚀 Быстрый старт

### Требования

- **Python 3.10+** — для backend
- **Node.js 18+** — для frontend

### Шаг 1: Установить Node.js (если ещё нет)

```bash
# macOS (через Homebrew)
brew install node

# Или скачай с https://nodejs.org/
```

### Шаг 2: Установить зависимости

```bash
# В корне проекта
cd /Users/valeriabolsaeva/Documents/Cursor2026/snake

# Python зависимости
pip install -r requirements.txt

# Frontend зависимости
cd frontend
npm install
cd ..
```

### Шаг 3: Запустить игру

Нужно открыть **ДВА терминала**:

#### Терминал 1 — Backend (API сервер)

```bash
cd /Users/valeriabolsaeva/Documents/Cursor2026/snake
python -m uvicorn app.main:app --reload --port 8000
```

Ты увидишь:
```
🐍 Snake Game API запущен!
📖 Документация: http://localhost:8000/docs
```

#### Терминал 2 — Frontend (игра в браузере)

```bash
cd /Users/valeriabolsaeva/Documents/Cursor2026/snake/frontend
npm run dev
```

Ты увидишь:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

### Шаг 4: Играть! 🎮

Открой в браузере: **http://localhost:5173**

---

## 🎮 Управление

| Клавиши | Действие |
|---------|----------|
| `↑` `↓` `←` `→` | Движение |
| `W` `A` `S` `D` | Движение (альтернатива) |
| `Space` или `P` | Пауза |

---

## 📁 Структура проекта

```
snake/
├── app/                    # 🐍 Backend (Python FastAPI)
│   ├── main.py            # Точка входа API
│   ├── database.py        # Подключение к SQLite
│   ├── models.py          # Модели базы данных
│   └── schemas.py         # Pydantic схемы
│
├── frontend/              # ⚛️ Frontend (React + Vite)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── hooks/         # Игровая логика
│   │   ├── styles/        # CSS стили
│   │   └── utils/         # API функции
│   └── package.json
│
├── data/                  # 💾 SQLite база данных
│   └── snake.db          # (создаётся автоматически)
│
└── docs/                  # 📚 Документация
```

---

## 🔧 API Эндпоинты

После запуска backend, документация доступна: http://localhost:8000/docs

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/health` | Проверка работоспособности |
| `GET` | `/api/leaderboard` | Таблица лидеров |
| `POST` | `/api/scores` | Сохранить результат |
| `GET` | `/api/scores/best` | Лучший результат игрока |
| `GET` | `/api/stats` | Общая статистика |

---

## 🛠️ Технологии

### Backend
- **FastAPI** — быстрый веб-фреймворк для Python
- **SQLite** — простая база данных (файл `data/snake.db`)
- **SQLAlchemy** — ORM для работы с базой данных
- **Pydantic** — валидация данных

### Frontend
- **React 18** — библиотека для UI
- **Vite** — быстрая сборка проекта
- **CSS Animations** — плавные анимации

---

## ❓ Частые вопросы

### Не запускается frontend?

1. Убедись что Node.js установлен: `node --version`
2. Установи зависимости: `cd frontend && npm install`

### Не сохраняются результаты?

1. Убедись что backend запущен (Терминал 1)
2. Проверь что он отвечает: http://localhost:8000/api/health

### Как сбросить таблицу лидеров?

Удали файл базы данных:
```bash
rm data/snake.db
```
База создастся заново при запуске backend.

---

## 📝 Лицензия

MIT — делай что хочешь 🎉

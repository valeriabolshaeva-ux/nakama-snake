# 🐍 Snake Game — Cursor Rules

> Правила для AI-агентов в Cursor IDE

---

## Проект

**Название:** Snake Game (Игра Змейка)
**Описание:** Классическая игра змейка с таблицей лидеров
**Стек:** 
- Backend: Python 3.11+, FastAPI, SQLite, Pydantic
- Frontend: React, Vite, CSS Animations
**Режим Git:** Pet Project (коммиты напрямую в main)

---

## Архитектура проекта

```
snake/
├── app/                    # Backend (Python FastAPI)
│   ├── __init__.py
│   ├── main.py            # Точка входа FastAPI
│   ├── database.py        # Подключение к SQLite
│   ├── models.py          # SQLAlchemy модели
│   └── schemas.py         # Pydantic схемы
│
├── frontend/              # Frontend (React)
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── hooks/         # Кастомные хуки
│   │   ├── styles/        # CSS стили
│   │   ├── App.jsx        # Главный компонент
│   │   └── main.jsx       # Точка входа
│   ├── package.json
│   └── vite.config.js
│
├── data/                  # SQLite база данных
│   └── snake.db
│
├── docs/                  # Документация
├── tests/                 # Тесты
└── scripts/               # Скрипты запуска
```

---

## Правила кодирования

### Python (Backend)

- **Версия:** Python 3.10+
- **Type hints:** ОБЯЗАТЕЛЬНЫ для всех функций
- **Docstrings:** ОБЯЗАТЕЛЬНЫ (Google style)
- **Именование:**
  - `PascalCase` — классы
  - `snake_case` — функции, переменные
  - `UPPER_CASE` — константы

### JavaScript/React (Frontend)

- **Версия:** ES6+, React 18+
- **Компоненты:** Функциональные компоненты с хуками
- **Стили:** CSS Modules или styled-components
- **Именование:**
  - `PascalCase` — компоненты (GameBoard.jsx)
  - `camelCase` — функции, переменные
  - `kebab-case` — CSS классы

---

## API Эндпоинты (Backend)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/api/health` | Проверка работоспособности |
| `GET` | `/api/leaderboard` | Получить таблицу лидеров |
| `POST` | `/api/scores` | Сохранить новый результат |
| `GET` | `/api/scores/best` | Лучший результат текущего игрока |

---

## Игровая механика

### Правила змейки:
1. Змейка движется непрерывно в текущем направлении
2. Управление: стрелки или WASD
3. При съедании еды: +1 к длине, +10 очков
4. Игра заканчивается при столкновении со стеной или собой
5. Скорость увеличивается с ростом очков

### Особенности:
- Плавные анимации движения
- Частицы при съедании еды
- Звуковые эффекты (опционально)
- Адаптивный дизайн

---

## Запуск проекта

### Backend (терминал 1):
```bash
cd /Users/valeriabolsaeva/Documents/Cursor2026/snake
source .venv/bin/activate  # если есть venv
uvicorn app.main:app --reload --port 8000
```

### Frontend (терминал 2):
```bash
cd /Users/valeriabolsaeva/Documents/Cursor2026/snake/frontend
npm install  # первый раз
npm run dev
```

### Открыть в браузере:
```
http://localhost:5173
```

---

## Git

### Формат коммитов

```
<type>(<scope>): <description>

Типы:
- feat: новая функциональность
- fix: исправление бага
- docs: документация
- style: форматирование/стили
- refactor: рефакторинг
```

### Примеры

```bash
feat(game): add snake movement logic
feat(ui): add leaderboard component
fix(game): correct collision detection
style(ui): improve game board animations
```

---

## Чеклист перед коммитом

- [ ] Игра запускается без ошибок
- [ ] Backend API отвечает корректно
- [ ] Нет console.error в браузере
- [ ] Type hints в Python коде
- [ ] Код отформатирован

---

## Запрещено

- ❌ Хардкодить секреты в код
- ❌ Коммитить node_modules/
- ❌ Коммитить .env файл
- ❌ Оставлять console.log в продакшн коде
- ❌ Игнорировать ошибки линтера
